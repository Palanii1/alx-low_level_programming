#include "main.h"

/**
 * _strlen - a function that returns the lenght of a string
 * @s: character
 * Return: string length
 */

int _strlen(char *s)
{
	int l;

	for (l = 0; s[l] != '\0'; l++)
	{

	}
	return (l);
}
